# network size
SIZE 10

# parameters of periodic execution
CYCLES 100
CYCLE SIZE*10000

# parameters of message transfer
# delay values here are relative to cycle length, in percentage,
# eg 50 means half the cycle length, 200 twice the cycle length, etc.
MINDELAY 0
MAXDELAY 0
# drop is a probability, 0<=DROP<=1
DROP 0

random.seed 1234567890
network.size SIZE
simulation.endtime CYCLE*CYCLES
simulation.logtime CYCLE

################### protocols ===========================

protocol.link peersim.core.IdleProtocol

protocol.counter video.emdc.eu.RecursivePeerCounter
protocol.counter.linkable link
protocol.counter.transport urt
protocol.counter.step CYCLE

protocol.urt UniformRandomTransport
protocol.urt.mindelay (CYCLE*MINDELAY)/100
protocol.urt.maxdelay (CYCLE*MAXDELAY)/100


################### initialization ======================

init.rndlink WireScaleFreeDM
init.rndlink.k 2
init.rndlink.protocol link

init.sch CDScheduler
init.sch.protocol counter
init.sch.randstart


################ control ==============================


